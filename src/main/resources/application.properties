# Environment
spring.profiles.active = dev

# Global Settings
server.port=8888

# Show database logs
spring.main.banner-mode=off
logging.level.org.springframework=ERROR

# mongodb
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=projetodb

